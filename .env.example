# Apple Music / MusicKit credentials — keep local, NEVER commit.
# Get these from https://developer.apple.com/account

# Your 10-character Team ID (Apple Developer portal → Membership)
APPLE_TEAM_ID=XXXXXXXXXX

# Key ID — matches the filename AuthKey_<KEY_ID>.p8
APPLE_KEY_ID=YYYYYYYYYY

# Path to the .p8 private key file (relative to project root)
APPLE_PRIVATE_KEY_PATH=./AuthKey_YYYYYYYYYY.p8

# ── OPTIONAL — Discord Rich Presence ────────────────────────────
# Leave blank to use Çatalify's default app (baked in discord-rpc.js).
DISCORD_CLIENT_ID=

# ── Release / publish ───────────────────────────────────────────
# GitHub Personal Access Token (classic, repo scope). Used by
# `npm run release` to upload installer + latest.yml to GitHub Releases.
GH_TOKEN=
