# Secrets — NEVER commit these
*.p8
.env
.env.local
secrets/
AuthKey_*.p8

# Dependencies
node_modules/

# Build outputs
dist/
dist-electron/
release/
out/

# Logs
*.log
npm-debug.log*

# Editor
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Electron
app.asar

# Cloudflare Workers (separate projects, deployed from local — keep the public
# app repo free of backend source). catalify-token signs Apple dev tokens;
# catalify-api is the social backend (Worker + D1).
catalify-token/
catalify-api/

# Misc
coverage/
.cache/
